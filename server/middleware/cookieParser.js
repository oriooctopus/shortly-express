var utils = require('../lib/hashUtils.js');

const parseCookies = (req, res, next) => {
  //create unique hash, dependent on username
  // var uniqueHash = 
  //insert hash into sessions table (maybe liked to users table)
};

module.exports = parseCookies;